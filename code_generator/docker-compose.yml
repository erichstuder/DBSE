services:
    main:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ..:/home/vscode/DBSE
        working_dir: /home/vscode/DBSE/code_generator
        stdin_open: true
        tty: true
        stop_grace_period: 0s
