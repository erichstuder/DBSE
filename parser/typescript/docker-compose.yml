services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/home/vscode/DBSE
    working_dir: /home/vscode/DBSE/parser/typescript
    stdin_open: true
    tty: true
    stop_grace_period: 0s
